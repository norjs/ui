/**
 * **NOTE!** This enum should correspond to identifiers in NrDatabaseJsType enum.
 *
 * @enum {string}
 * @readonly
 */
export const NrDatabaseType = {

    NULL        : "NULL",
    TIMESTAMPTZ : "TIMESTAMPTZ",
    TEXT        : "TEXT",
    INT         : "INT",
    BIGINT      : "BIGINT",
    BOOLEAN     : "BOOLEAN",
    UUID        : "UUID"

};

export default NrDatabaseType;
